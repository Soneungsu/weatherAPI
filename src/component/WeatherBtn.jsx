import React from "react";

const WeatherBtn = () => {
  return (
    <div>
      <button onClick={() => {}}>일본</button>
      <button>러시아</button>
      <button>제주도</button>
    </div>
  );
};

export default WeatherBtn;
